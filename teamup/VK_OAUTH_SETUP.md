# Настройка VK OAuth с popup и postMessage

## ✅ Что было реализовано

Реализовано корректное решение для VK OAuth авторизации с использованием popup окна и `postMessage` для передачи данных между окнами. Это гарантирует, что `localStorage.setItem('token')` выполняется в контексте основного сайта, а не popup окна.

## 📁 Созданные файлы

1. **`src/app/auth/vk-callback/page.tsx`** - Callback страница, которая открывается в popup после авторизации VK и отправляет данные в основное окно через `postMessage`

2. **`src/utils/vkAuth.ts`** - Утилита для открытия popup и обработки сообщений от callback страницы

3. **`src/utils/getBaseUrl.ts`** - Утилита для получения базового URL приложения (работает как на клиенте, так и на сервере)

## 🔄 Как это работает

1. **Пользователь нажимает "Войти через VK"**
   - Открывается popup окно с VK авторизацией
   - Основное окно остается открытым в фоне

2. **VK перенаправляет на callback страницу**
   - VK редиректит на `/auth/vk-callback` в popup окне
   - Callback страница извлекает `vkId` из URL hash

3. **Callback отправляет данные в основное окно**
   - Используется `window.opener.postMessage()` для отправки `vkId` в основное окно
   - Popup закрывается автоматически

4. **Основное окно получает данные**
   - Утилита `openVkAuthPopup` получает сообщение через `postMessage`
   - Выполняется запрос на сервер: `authService.loginWithVk(vkId)`

5. **Токен сохраняется в правильном origin**
   - `localStorage.setItem("token", token)` выполняется в контексте **основного окна**
   - Токен сохраняется в localStorage вашего сайта (`teamup-579l.vercel.app`)
   - Пользователь добавляется в Zustand store
   - Происходит редирект на главную страницу

## ⚙️ Важные настройки

### 1. Настройка Redirect URI в VK приложении

В настройках вашего VK приложения (https://vk.com/apps?act=manage) необходимо добавить:

**Redirect URI для production:**
```
https://teamup-579l.vercel.app/auth/vk-callback
```

**Redirect URI для локальной разработки (опционально):**
```
http://localhost:3000/auth/vk-callback
```

### 2. Переменные окружения (опционально)

Если хотите явно указать URL приложения, можете добавить в `.env.local`:

```env
NEXT_PUBLIC_APP_URL=https://teamup-579l.vercel.app
```

Но это не обязательно, так как на клиенте всегда используется `window.location.origin`.

## 🔍 Проверка работы

После настройки проверьте:

1. ✅ При нажатии на "Войти через VK" открывается popup
2. ✅ После авторизации popup закрывается
3. ✅ Токен сохраняется в localStorage вашего сайта (DevTools → Application → Local Storage → `https://teamup-579l.vercel.app`)
4. ✅ Пользователь редиректится на главную страницу
5. ✅ Данные пользователя сохраняются в Zustand store

## 🐛 Отладка

Если что-то не работает:

1. **Проверьте консоль браузера** - там будут логи ошибок
2. **Проверьте Redirect URI** - он должен точно совпадать в VK настройках и в коде
3. **Проверьте блокировщик popup** - убедитесь, что браузер не блокирует popup окна
4. **Проверьте Network tab** - убедитесь, что запрос на сервер выполняется успешно

## 📝 Основные изменения в коде

### Страница логина (`src/app/auth/login/page.tsx`)
- Убрана обработка hash в URL (больше не нужна)
- Добавлен вызов `openVkAuthPopup()` вместо прямого редиректа
- Обработка результата через Promise

### Страница регистрации (`src/app/auth/register/page.tsx`)
- Аналогичные изменения как на странице логина

### Хуки (`src/hooks/useVkLogin.tsx`, `src/hooks/useVkRegister.tsx`)
- Добавлены комментарии о том, что `localStorage.setItem` выполняется в контексте основного окна
- Используется Next.js router для редиректа

## ✨ Преимущества этого подхода

1. ✅ **Токен всегда сохраняется в правильном origin** - проблема решена!
2. ✅ **Лучший UX** - пользователь не покидает основную страницу
3. ✅ **Безопасность** - проверка origin при получении сообщений
4. ✅ **Удобство** - автоматическое закрытие popup после авторизации

## 🚀 Готово к использованию!

Решение полностью готово к работе. Не забудьте только добавить Redirect URI в настройках VK приложения.
